<a href="{{ $link }}">
    <div class="grid__content__indiv">
        <div class="img-container">
            <img src="{{ $img_url }}" alt="{{ $title }}" />
        </div>

        <h3 class="sm-heading">{{ $title }}</h3>
        <p class="copy">{!!$text!!}</p>
    </div>
</a>